
/**
 * 技能流程模块基类
 * 1、condition：条件模块，返回false/true,条件提供可选项配置，如：最低蓝量、最低(高)血量、cd、状态限制(受击、禁锢状态是否可释放等)
 * 2、timer：定时器模块(定时触发，等待触发)
 * 3、trigger：触发器(执行)模块基类,立即执行某一动作,如前摇、开始施法、伤害检测、结束施法
 */
export default class skillControlBase{
    
}
